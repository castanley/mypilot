# mypilot-realtime (scaffold)

Reserved for a future extraction of the WebSocket/presence layer into a dedicated service.
For Milestones 1–2 the realtime endpoints (`WS /api/realtime/device`, `WS /api/realtime/web`)
and Redis presence live inside `mypilot-stack/api`. Do not start this service yet.
